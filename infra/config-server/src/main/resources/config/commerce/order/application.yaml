server:
  port: 0

spring:
  application:
    name: order
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: always
  datasource:
    driver-class-name: org.postgresql.Driver
    url: 'jdbc:postgresql://localhost:10432/order_db'
    username: postgres
    password: 12345

javax:
  persistence:
    validation:
      group:
        pre-update: ru.yandex.practicum.OnUpdate